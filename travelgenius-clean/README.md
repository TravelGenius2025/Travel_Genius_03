# Clean Next.js (CJS) starter for TravelGenius

- CommonJS configs only (`postcss.config.cjs`, `tailwind.config.cjs`, `next.config.js`)
- No `'type':'module'` in package.json
- Client files start with `'use client'`
